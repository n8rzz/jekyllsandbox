  <div class="span6">
    <h4><i class="icon-certificate"> </i> Related Posts <small>They might not be good...</small></h4>
    {% if site.related_posts == empty %}
      <p>There are no related posts. How sad...</p>
    {% else %}
      <ul class="unstyled">
      {% for post in site.related_posts limit:3 %}
        <li><a href="{{post.url}}"><i class="icon-file"> </i> {{post.title}}</a> - {{post.date | date_to_long_string }}</li>
      {% endfor %}
      </ul>
    {% endif %}
  </div>
  
  <div class="span3">
  {% unless page.categories == empty %}
    <h4><i class="icon-folder-open"> </i> Categories</h4>
    <ul class="unstyled">
      {% assign categories_list = page.categories %}
      {% include JB/categories_list %}
    </ul>
  {% endunless %}
  </div>

  <div class="span3">
  {% unless page.tags == empty %}
    <h4><i class="icon-tags"> </i> Tags</h4>
    <ul class="unstyled">
      {% assign tags_list = page.tags %}
      {% include JB/tags_list %}
    </ul>
  {% endunless %}  
  </div>


  ---


  <!-- Categories -->
      {% unless page.categories == empty %}
    <ul class="categories">
        <li><i class="icon-drawer"></i></li>
        {% assign categories_list = page.categories %}
        
        {% if categories_list.first[0] == null %}
        {% for category in categories_list %} 
          <li data-size="{{ site.categories[category].size }}"><a href="/categories/#{{ category }}">
            {{ category | join: "/" }}
          </a></li>
        {% endfor %}
      {% else %}
        {% for category in categories_list %} 
          <li data-size="{{ category[1].size }}"><a href="/categories/#{{ category[0] }}">
            {{ category[0] | join: "/" }}
          </a></li>
        {% endfor %}
      {% endif %}
        {% assign categories_list = nil %}
    
    </ul>
    {% endunless %} 